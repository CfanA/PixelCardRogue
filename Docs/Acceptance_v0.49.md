# 《云海邮差》v0.49 路线决策与构筑快照验收

> 验收日期：2026-07-26  
> 版本：0.49.0  
> 范围：风险交易、事件三分支、补给站删牌与卡牌分支改写、构筑快照  
> 数值状态：沿用既有基线，未进入系统性平衡

## 验收目标

在v0.48三幕结构上补足第二幕与路线事件中的构筑取舍，并把关键决策前后的完整构筑状态持久化到单局存档和邮政档案，为v0.50失败复盘、构筑总结与再次出发流程提供可直接读取的数据基础。

## 路线决策

| 验收项 | 结果 |
|---|---|
| 信标纪事选项 | 三段事件均提供协作、机会、静默三分支 |
| 静默路线代价 | 必须从当前牌组实际删除1张副本，确认前可以返回事件 |
| 静默开局 | 删除1张牌，获得18枚邮票，进入“广播封存”状态 |
| 静默中段 | 删除1张牌，获得12枚邮票，合同开局核心写入B分支 |
| 静默终章 | 删除1张牌，优先安装合同适配模块；模块已满时获得25枚邮票 |
| 终局阵营 | “无声航标”以中立阵营进入双首领矩阵 |
| 补给站删牌 | 第一站18枚、第二站28枚；每站限1次，不设牌组或伤害牌下限 |
| 卡牌分支改写 | 第一站28枚、第二站38枚；可选择任意现有卡牌并写入/覆盖A或B分支 |
| 补给站维修 | 保留20枚邮票恢复12点机体的既有服务；与两项工坊服务分别计次 |

## 构筑快照

| 验收项 | 结果 |
|---|---|
| 写入时机 | 离港、永久改装、事件、奖励、维修坞、商店服务、终局整备、首领进场前 |
| 构筑字段 | 牌组全部副本、强化卡、A/B分支、模块 |
| 资源字段 | 机体、货物完整度、邮票、机体改装、纪事状态 |
| 路线字段 | 快照键、UTC时间、幕、航线列、航点、写入类型 |
| 数量边界 | 同键覆盖，按时间序保留，最多32份 |
| 失败与送达 | 最近运行记录均保存整局快照序列 |
| 数据隔离 | 保存、归档与运行态之间使用深拷贝，列表不会互相引用 |

## 兼容与恢复

- 单局存档升级至v8，新增工坊选中卡牌、补给站删牌/改写使用状态与构筑快照。
- v1—v6沿用既有迁移链；v7迁移时初始化空快照和空工坊上下文，不追补历史决策。
- 邮政档案升级至v5；v1—v4迁移后，历史运行保留原统计并使用空快照列表。
- 从事件删牌、商店删牌、工坊卡牌清单或A/B分支确认界面退出后，继续配送会恢复对应上下文。
- 删除最后一张同名副本仍会同步清理该卡的强化与分支数据，快照记录删除后的真实牌组。

## 自动验证

| 验证 | 结果 |
|---|---|
| Unity 6000.5.3f1 运行时编译 | 通过 |
| Unity 6000.5.3f1 编辑器编译 | 通过 |
| 108卡池门禁 | `SKY_COURIER_CARD_POOL_VALIDATION_COMPLETE`；108种、42种直接伤害、68种扩展效果 |
| 核心规则回归 | `SKY_COURIER_RULE_VALIDATION_COMPLETE` |
| v8单局存档往返 | 工坊上下文、服务状态与完整快照字段纳入门禁 |
| v7迁移 | 工坊选中值重置为-1，快照初始化为空列表 |
| 档案v5 | 快照随运行记录写入、备份恢复并保持字段完整 |
| 静默纪事 | 三段状态、未完成/已完成边界和中立首领阵营纳入门禁 |
| 决策价格 | 两站删牌、分支改写与三段静默收益纳入门禁 |
| 快照隔离 | 深拷贝后修改副本不会污染原始构筑 |
| Windows发布构建 | Unity `BuildResult.Succeeded`；`SKY_COURIER_BUILD_COMPLETE|version=0.49.0` |
| 独立EXE启动 | 通过；独立进程保持运行8秒，Player日志异常、断言与崩溃匹配0 |
| Git空白检查 | 通过 |

## 发布交付物

- 发布目录：`Builds/SkyCourierPrototype_v0.49`
- 启动入口：`Builds/SkyCourierPrototype_v0.49/Sky Courier Prototype.exe`
- 验收文本：`Builds/SkyCourierPrototype_v0.49/验收报告_v0.49.txt`
- EXE大小：667648字节
- 完整构建大小：188017143字节
- EXE SHA-256：`5AE0C84993C6BFF7D0F03166CAFD148CDEC61F67EC85B5D97600E04B2ABB26E4`
- 本轮不生成ZIP。

Unity程序运行仍依赖EXE旁的`_Data`目录、`UnityPlayer.dll`、`MonoBleedingEdge`和其他旁置运行库；验收只把EXE与本报告作为交付入口，不额外生成压缩包。

## 未执行项

- 不运行`BalanceSimulator`，不刷新9套自动构筑或18条路线的通关率。
- 不为提高胜率修改卡牌、敌机、合同、战斗奖励、商店卡价格或既有经济曲线。
- 新增事件收益与工坊价格属于路线决策初稿，统一留到v0.50结合失败复盘与构筑快照进行系统性平衡。

## 下一阶段

v0.50直接读取v0.49快照序列，在失败页和送达页展示牌组、强化、模块与资源的关键变化，形成构筑总结、失败归因和再次出发闭环；随后再用真人试玩和自动模拟共同校准卡池、敌方压力与风险交易数值。
